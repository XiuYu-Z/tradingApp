package controller.forms;

public class FreezeUserForm {

    private int userId;

    /**
     * Gets the user id we are going to freeze.
     *
     * @return the user id we are going to freeze.
     */
    public int getUserId() {
        return this.userId;
    }

    /**
     * Sets the user id we are going to freeze.
     *
     * @param userId the user id we are going to freeze.
     */
    public void setUserId(int userId) {
        this.userId = userId;
    }

}
